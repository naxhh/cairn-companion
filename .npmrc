# Obsidian requires release tags to exactly match manifest.json's version
# (e.g. "1.1.0"), with no "v" prefix. npm's default tag-version-prefix is
# "v", which `npm version <bump>` would otherwise silently apply, producing
# a tag Obsidian's plugin build/release check rejects.
tag-version-prefix=
